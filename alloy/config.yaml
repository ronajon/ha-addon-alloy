name: Grafana Alloy
version: "1.1.0"
slug: alloy
description: >-
  Ship Home Assistant logs to Loki using Grafana Alloy.
  Replacement for the deprecated Promtail add-on.
  Reads systemd journal and forwards to a remote Loki instance.
url: https://github.com/ronajon/ha-addon-alloy
codeowners:
  - "@ronajon"
arch:
  - aarch64
  - amd64
init: false
startup: services
boot: auto
journald: true
map:
  - addon_config:rw
ports:
  12345/tcp: 12345
ports_description:
  12345/tcp: "Alloy debug UI"
watchdog: "http://[HOST]:[PORT:12345]/-/ready"
options:
  loki_url: "http://localhost:3100/loki/api/v1/push"
  log_level: info
  disable_reporting: true
schema:
  loki_url: url
  log_level: "list(debug|info|warn|error)"
  additional_config: "str?"
  label_overrides: "str?"
  disable_reporting: bool
stage: experimental
apparmor: false
